

const user = {
    username : 'andres',
    email: 'correo@google.com',
    age : 20,
    ranking: 9,
}
const username = user.username;
const ranking = user.username;
const age = user.username;

//const{ username, ranking} = user ;

console.log(`${username} tiene ${age} de edad`);

console.log(ranking);